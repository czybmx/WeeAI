【ALL OF THIS README WAS GENERATED BY GPT-4-O1-PREVIEW /// 以下 README 内容均使用 GPT-4-O1-PREVIEW 生成】
【JUST A HOBBY, NO MONEY EARNED FROM THIS /// 兴趣使然，没有任何收费】
【MAY CONTAIN MANY BUGS /// 或许会有很多漏洞】
【I'M NOT GOOD AT CODING, NO FORMAL EDUCATION, SELF-TAUGHT AT HOME /// 我的代码基础很差，没上过相关学校或课程，自学而成】
【THE CODE WAS JOINTLY GENERATED BY ME USING CHATGPT AND CLAUDE.AI /// 代码是由我使用 CHATGPT 和 CLAUDE.AI 联合生成】

# WeeAI Chat Application

## Description

WeeAI is an AI-powered chat application built with Streamlit, integrating multiple AI models and services to provide a rich conversational experience. It allows users to interact with an AI assistant that can generate text responses, voice responses, and images based on the conversation context.

## Features

- **Conversational AI Assistant**: Engage in a conversation with an AI assistant that can understand and respond to user inputs.
- **Voice Response**: The assistant can generate voice responses using a text-to-speech (TTS) service.
- **Image Generation**: The assistant can generate images based on the conversation, using a Stable Diffusion model.
- **Chat History Management**: Save, load, and delete chat histories. Manage multiple conversations.
- **Knowledge Base**: Uses a knowledge base to store conversation overflow and retrieve relevant context.

## Requirements

- **Python 3.7+**

- **Python Packages**:
  - `streamlit`
  - `requests`
  - `sentence-transformers`
  - `sounddevice`
  - `soundfile`
  - `numpy`
  - `Pillow`

- **External Services**:
  - **Ollama Server**: For AI text generation. Should be running at `[http://localhost:11434/api/generate]`.
  - **TTS Server**: For text-to-speech. Should be running at `[http://127.0.0.1:9880/tts]`.【IM USING GPT-SoVITS-v2-240821】：：：[https://github.com/RVC-Boss/GPT-SoVITS?tab=readme-ov-file]
  - **Stable Diffusion API**: For image generation. Should be running at `[http://127.0.0.1:9999/sdapi/v1/txt2img]`.【IM USING SD-FORGE-WEBUI】：：：[https://github.com/lllyasviel/stable-diffusion-webui-forge?tab=readme-ov-file]

- **Audio Output Device**: Ensure that an audio output device is available and correctly configured.

## Installation

1. **Clone the Repository**

   ```bash
   git clone https://github.com/yourusername/weeai.git
   cd weeai
   ```

2. **Install Python Packages**

   Install the required Python packages using pip:

   ```bash
   pip install streamlit requests sentence-transformers sounddevice soundfile numpy Pillow
   ```

3. **Set Up External Services**

   - **Ollama Server**: Install and run the Ollama server for AI text generation.
   - **TTS Server**: Install and run the TTS server.
   - **Stable Diffusion API**: Install and run the Stable Diffusion API server.

4. **Configure Paths and Settings**

   - Update the paths and settings in the code as needed.
   - For example, set the correct paths for `REF_AUDIO_PATH` and `GENERATED_IMAGES_DIR`.

## Usage

1. **Run the Application**

   Start the Streamlit application:

   ```bash
   streamlit run app.py
   ```

2. **Interact with the AI Assistant**

   - Open the web interface provided by Streamlit.
   - Enter your messages in the chat input.
   - Enable or disable voice and image generation using the checkboxes in the sidebar.
   - Manage chat histories from the sidebar.

3. **Voice and Image Generation**

   - If voice generation is enabled, the assistant will generate audio responses.
   - If image generation is enabled, the assistant may generate images based on the conversation context.

## Notes

- Ensure that all external services are running and accessible at the specified URLs.
- Modify the code as needed to match your environment and configurations.

---

# WeeAI 聊天应用程序

## 描述

WeeAI 是一个由 Streamlit 构建的 AI 聊天应用程序，集成了多个 AI 模型和服务，提供丰富的对话体验。它允许用户与 AI 助手互动，助手可以根据对话上下文生成文本回复、语音回复和图像。

## 功能

- **对话式 AI 助手**：与 AI 助手进行对话，它能够理解并回复用户输入。
- **语音回复**：助手可以使用文本到语音（TTS）服务生成语音回复。
- **图像生成**：助手可以基于对话内容生成图像，使用 Stable Diffusion 模型。
- **聊天历史管理**：保存、加载和删除聊天历史。管理多个对话。
- **知识库**：使用知识库存储溢出的对话并检索相关的上下文。

## 要求

- **Python 3.7+**

- **Python 包**：
  - `streamlit`
  - `requests`
  - `sentence-transformers`
  - `sounddevice`
  - `soundfile`
  - `numpy`
  - `Pillow`

- **外部服务**：
  - **Ollama 服务器**：用于 AI 文本生成。应在 `[http://localhost:11434/api/generate]` 运行。
  - **TTS 服务器**：用于文本到语音。应在 `[http://127.0.0.1:9880/tts]` 运行。【我使用GPT-SoVITS-v2-240821】：：：[https://github.com/RVC-Boss/GPT-SoVITS/blob/main/docs/cn/README.md]
  - **Stable Diffusion API**：用于图像生成。应在 `[http://127.0.0.1:9999/sdapi/v1/txt2img]` 运行。【我使用SD-FORGE-WEBUI】：：：[https://github.com/lllyasviel/stable-diffusion-webui-forge?tab=readme-ov-file]

- **音频输出设备**：确保音频输出设备可用且配置正确。

## 安装

1. **克隆仓库**

   ```bash
   git clone https://github.com/yourusername/weeai.git
   cd weeai
   ```

2. **安装 Python 包**

   使用 pip 安装所需的 Python 包：

   ```bash
   pip install streamlit requests sentence-transformers sounddevice soundfile numpy Pillow
   ```

3. **设置外部服务**

   - **Ollama 服务器**：安装并运行 Ollama 服务器，用于 AI 文本生成。
   - **TTS 服务器**：安装并运行 TTS 服务器。
   - **Stable Diffusion API**：安装并运行 Stable Diffusion API 服务器。

4. **配置路径和设置**

   - 根据需要更新代码中的路径和设置。
   - 例如，设置 `REF_AUDIO_PATH` 和 `GENERATED_IMAGES_DIR` 的正确路径。

## 使用

1. **运行应用程序**

   启动 Streamlit 应用程序：

   ```bash
   streamlit run app.py
   ```

2. **与 AI 助手互动**

   - 打开 Streamlit 提供的网页界面。
   - 在聊天输入框中输入您的消息。
   - 使用侧边栏中的复选框启用或禁用语音和图像生成。
   - 在侧边栏中管理聊天历史。

3. **语音和图像生成**

   - 如果启用了语音生成，助手将生成语音回复。
   - 如果启用了图像生成，助手可能会根据对话上下文生成图像。

## 注意

- 确保所有外部服务正在运行并可通过指定的 URL 访问。
- 根据需要修改代码以匹配您的环境和配置。
